<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper 
    PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN" 
    "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">
<mapper namespace="redeemorderAttachment">

	<parameterMap id="redeemorderAttachment" type="com.sw.plugins.market.order.entity.RedeemOrderAttachment" />

	<resultMap id="redeemorderAttachmentResult" type="com.sw.plugins.market.order.entity.RedeemOrderAttachment">
		<result property="id" column="ID" />
		<result property="title" column="Title" />
		<result property="fileName" column="FileName" />
		<result property="fileUrl" column="FileUrl" />
		<result property="uploadTime" column="UploadTime" />
		<result property="audited" column="Audited" />
		<result property="orderNumber" column="OrderNumber" />
	</resultMap>
	
	
	<!-- 保存 -->
	<insert id="insert" parameterMap="redeemorderAttachment" >
		insert into TC_RedeemOrderAttachment
		<trim prefix="(" prefixOverrides="," suffix=")">
			<if test="orderNumber!=null and orderNumber!=''">,OrderNumber</if>
			<if test="title!=null and title!=''">,Title</if>
			<if test="fileName!=null and fileName!=''">,FileName</if>
			<if test="fileUrl!=null and fileUrl!=''">,FileUrl</if>		
			,UploadTime
			<if test="audited!=null">,Audited</if>	
		</trim>
		values
		<trim prefix="(" prefixOverrides="," suffix=")">
			<if test="orderNumber!=null and orderNumber!=''">,#{orderNumber}</if>
			<if test="title!=null and title!=''">,'${title}'</if>
			<if test="fileName!=null and fileName!=''">,'${fileName}'</if>
			<if test="fileUrl!=null and fileUrl!=''">,'${fileUrl}'</if>		
			,now()
			<if test="audited!=null">,${audited}</if>	
		</trim>
	</insert>
</mapper>
